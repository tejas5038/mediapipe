# Legacy OSS worker intentionally disabled.
# The centralized worker is .github/workflows/oss-agent-worker-active.yml.
name: OSS Agent Worker Legacy Disabled

on:
  workflow_dispatch:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy worker disabled; use OSS Agent Worker Active."
